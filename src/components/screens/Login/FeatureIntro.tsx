import { Typography } from "@mui/material";
import { Box } from "@mui/system";

export function FeatureIntro() {
  return (
    <Box>
      <Typography sx={{ paddingTop: "1em" }}>
        Core features include: manage raid times, members, and roster.
      </Typography>
    </Box>
  );
}
