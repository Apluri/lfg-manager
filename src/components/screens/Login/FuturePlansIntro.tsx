import { Typography } from "@mui/material";
import { Box } from "@mui/system";

export function FuturePlansIntro() {
  return (
    <Box>
      <Typography sx={{ paddingTop: "1em" }}>
        Future plans: calendar view for raids, raid categories for easy
        filtering, raid party management and discord login with discord
        notifications.
      </Typography>
    </Box>
  );
}
